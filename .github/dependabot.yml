version: 2
updates:
  # Security updates still open via Dependabot alerts; routine version bumps off.
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 0
    versioning-strategy: "lockfile-only"
