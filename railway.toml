[build]
dockerfilePath = "packages/twenty-docker/twenty/Dockerfile.railway-server"

[deploy]
startCommand = "node dist/main"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "on-failure"
